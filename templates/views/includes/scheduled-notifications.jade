table.table.table-striped#scheduled-notifications
    tr
        th
        th Message
        th Time To Send
        th Ministries to Send To
    each notification in notifications
        tr 
            td                                     
                a(href='/keystone/notifications/' + notification._id)
                    span.glyphicon.glyphicon-pencil
            td= notification.message
             - var date = new Date(notification.time.toISOString().replace("Z", "+0800"))
            td= date.toLocaleDateString('en-US', {timezone: 'CA', month: '2-digit', day: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit'})
            td 
                if notification.ministries.length
                    select
                        each ministry in notification.ministries
                            option= ministry.name
                else
                    option Everyone